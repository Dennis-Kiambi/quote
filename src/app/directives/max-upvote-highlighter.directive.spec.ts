import { MaxUpvoteHighlighterDirective } from './max-upvote-highlighter.directive';

describe('MaxUpvoteHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxUpvoteHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
